meta {
  name: Delete account
  type: graphql
  seq: 5
}

post {
  url: {{host}}/graphql
  body: graphql
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:graphql {
  mutation {
    deleteAccount(
        password: "Ismael12345"
    ) {
      ok
    }
  }
}
