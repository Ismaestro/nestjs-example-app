meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{host}}/v1/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "maverick@topgun.com",
    "password": "Secret42"
  }
}

tests {
  var headerValue = res.getBody().accessToken;
  bru.setEnvVar('token', headerValue);
}
