meta {
  name: Refresh Token
  type: http
  seq: 6
}

post {
  url: {{host}}/{{version}}/user/token/refresh
  body: json
  auth: none
}

script:pre-request {
  const refreshToken = bru.getEnvVar('refreshToken');
  if (refreshToken) {
    req.headers.cookie = `refreshToken=${refreshToken}`;
  }
}
