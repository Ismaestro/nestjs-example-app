meta {
  name: Update
  type: http
  seq: 5
}

patch {
  url: {{host}}/{{version}}/user
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "firstname": "Ismael Ramos",
    "language": "es-ES"
  }
}
